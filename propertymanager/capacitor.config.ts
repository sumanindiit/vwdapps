import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.propertymanager.vwd',
  appName: 'VWD property manager',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
