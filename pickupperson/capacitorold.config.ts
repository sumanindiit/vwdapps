import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vwdpickup.app',
  appName: 'VWDpickup',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
